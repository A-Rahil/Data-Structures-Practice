Get kth element Method
template <class Type>
Type linkedListType<Type>::getKThElement(int k) {
    assert (k >= 0 && k < count);
    nodeType *current = head;
    for (int i = 0; i < k; i++) {
        current = current->link;
    }
    return current->info;
}